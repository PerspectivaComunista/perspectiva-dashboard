import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Perspectiva Comunistă - Autentificare",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
